@model List<NewsCategory>
@using Nop.Core.Domain.News
@{/*
@if (Model.NewsItems.Count > 0)

    model HomePageNewsItemsModel
using Nop.Web.Models.News;
    <div class="blog-post-area">
        <h2 class="title text-center">@T("News")</h2>
        <div class="custom-page-box-div">
            @foreach (var item in Model.NewsItems)
            {

                <div class="single-blog-post">
                    <h3><a class="news-title" href="@Url.RouteUrl("NewsItem", new { SeName = item.SeName })">
                                        @item.Title</a></h3>
                    <div class="post-meta">
                        <ul>
                            
                            <li><i class="fa fa-clock-o"></i> @item.CreatedOn.ToString("T")</li>
                            <li><i class="fa fa-calendar"></i> @item.CreatedOn.ToString("D")</li>
                        </ul>
                        
                    </div>
                    
                    <p>@Html.Raw(item.Short)</p>
                    <a  class="btn btn-primary" href="@Url.RouteUrl("NewsItem", new { SeName = item.SeName })">@T("News.MoreInfo")</a>
                </div>


                
                <br/>

                <legend></legend>

            }
        <div class="view-all pull-text-right">
            <a href="@Url.RouteUrl("NewsArchive")" class="btn btn-primary">
                @T("News.ViewAll")</a>
        </div>

        </div>
    </div>
<br/>

 */}
<style>
    .home-info-body {
        word-break: break-all;
        word-wrap: break-word;
        width: 100%;
        text-indent: 2em;
        height: 140px;
    }

    .home-info-footer {
        height: 215px;
    }

    .home-info-img {
        height: 160px;
    }

    .more {
        text-decoration: none;
        color: #535B60;
        font-size: 14px;
    }

        .more:hover {
            text-decoration: none;
            cursor: pointer;
        }

    .list-group {
        border-left: 1px solid #DDD;
    }

    .list-group-item:first-child {
        border-top-right-radius: 0px;
        border-top-left-radius: 0px;
    }
    /*
    .list-group-item:last-child {
        border-top: 1px solid #eee;
    }*/

    .list-group-item {
        width: 100%;
        text-overflow: ellipsis;
        white-space: nowrap;
        overflow: hidden;
        border: none;
    }

        .list-group-item img {
            width: 3px;
            height: 3px;
            margin-right: 4px;
        }

    .tab-content, .nav-tabs > li.active > a {
        box-shadow: 0px 0px 3px #999;
    }

    .nav-tabs {
        background-color: #f5f5f5;
        box-shadow: 0px 0px 3px #999;
    }
    .article-time {
    position:absolute;
    right:2px;
    font-size:smaller;
  
    }
    .articel-content {
        text-overflow: ellipsis;
        overflow: hidden; 
           
    }
</style>
<div class="container">
    <div class="row" style="margin-left:0px;">
        <!--左侧分页-->
        <div class="col-md-6">
            <ul class="nav nav-tabs" role="tablist">
                @{/*foreach (var item in Model.Take(3)) { 
                    <li role="presentation" class=""><a href="#@item.DomId" data-toggle="tab">@item.Name</a></li>
                }*/
                }
                <li role="presentation" class="active"><a href="#OD" data-toggle="tab">工业设计</a></li>
                <li role="presentation" class=""><a href="#SS" data-toggle="tab">CAE仿真</a></li>
                <li role="presentation" class=""><a href="#3DEC" data-toggle="tab">电子商务</a></li>
            </ul>
            <div class="tab-content">
                 @{Html.RenderAction("ListByCategory", "news", new { category = 6, count = 8, flag = true });}
                 @{Html.RenderAction("ListByCategory", "news", new {category=3,count=8 });}
                 @{Html.RenderAction("ListByCategory", "news", new { category = 5, count = 8 });}
            </div>
        </div>
        <!--右侧分页-->
        <div class="col-md-6">
            <ul class="nav nav-tabs" role="tablist">
               @{/*foreach (var item in Model.Skip(3).Take(3))
              {
                 <li role="presentation" class=""><a href="#@item.DomId" data-toggle="tab">@item.Name</a></li>
              }*/}
                <li role="presentation" class="active"><a href="#3DPrint" data-toggle="tab">3D打印</a></li>
                <li role="presentation" class=""><a href="#3DScan" data-toggle="tab">3D扫描</a></li>
            </ul>
            <div class="tab-content">
               @{Html.RenderAction("ListByCategory", "news", new { category = 2, count = 8, flag = true });}
                @{Html.RenderAction("ListByCategory", "news", new { category = 1, count = 8 });}
            </div>
        </div>
    </div>
</div>